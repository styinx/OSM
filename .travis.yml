dist: bionic
language: cpp

os:
  - linux

compiler:
  - clang
  - gcc

cache:
  directories:
    - $HOME/.cache

addons:
  apt:
    packages:
    - pip3
    - cmake
    - protobuf-compiler
    - libprotobuf-dev

jobs:
  include:
    - stage: test
      script: ./scripts/test.sh

install:
  - ./scripts/install.sh

script:
  - ./scripts/compile.sh
